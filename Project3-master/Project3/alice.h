#pragma once
#include <iostream>
#include <string>
#include "Player.h"
class alice : protected Player
{
public:
	alice();
	alice(string Name, int score, int roundScore, bool isInGame) {
		

	};
	~alice();
};

